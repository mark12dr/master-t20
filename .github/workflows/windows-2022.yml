name: RDP - Windows Server 2022

on:
  workflow_dispatch

jobs:
  start-rdp:
    runs-on: windows-2022
    timeout-minutes: 360

    steps:
      - name: 🧰 تحميل Ngrok وتهيئة الملفات
        run: |
          Invoke-WebRequest https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip
          Expand-Archive ngrok.zip -DestinationPath ngrok

      - name: 🔐 تسجيل الدخول إلى ngrok
        run: .\ngrok\ngrok.exe authtoken $Env:NGROK_AUTH
        env:
          NGROK_AUTH: ${{ secrets.TAKEN }}

      - name: 🖥️ تفعيل RDP وضبط كلمة المرور
        run: |
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          net user runneradmin Aa123456!

      - name: 🚪 بدء نفق ngrok على المنفذ 3389
        run: .\ngrok\ngrok.exe tcp 3389
